extends layout

block content
  h2 #{title}
  h3 #{message}
  table
      - for (var r in rows)
        tr
            td= rows[r].type
            td= rows[r].status
            td= moment(rows[r].datetime).format("MMMM Do YYYY, h:mm:ss a")
  br
    